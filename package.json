{
  "name": "react-node-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && yarn start\" \"cd frontend && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "yarn && cd backend && yarn && cd .. && cd frontend && yarn"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
